name: api
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  http_parser: ^4.0.2
  mime: ^1.0.4

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
